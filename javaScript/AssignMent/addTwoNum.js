// JavaScript Program to Add Two Numbers

let firstNum = parseInt(prompt("Enter the FirstNum"));
let secondNum = parseInt(prompt("Enter the secondNum"));

let addFun = (first , second) => {
    return first+second
}

console.log(addFun(firstNum, secondNum));