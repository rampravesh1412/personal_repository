let triangleHeight = parseFloat(prompt("Enter the triangleHeight"));
let triangleWeighth = parseFloat(prompt("Enter the triangleWeighth"));

let areOfTriangle = 1/2 * triangleWeighth * triangleWeighth;
console.log(areOfTriangle);